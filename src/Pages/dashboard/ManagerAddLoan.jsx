import React from "react";

/**
 * Manager Add Loan - placeholder UI.
 * Replace with react-hook-form and image upload, EMI plan inputs.
 */

const ManagerAddLoan = () => {
  return (
    <div>
      <h2 className="text-xl md:text-2xl font-bold mb-3">Add Loan</h2>
      <p className="text-sm text-base-content/70 mb-4">Build a form to create loan products (title, desc, interest, max limit, category, EMI plans, images).</p>

      <div className="p-4 border border-base-200 rounded-lg">
        <p className="text-xs">Form UI to be implemented with react-hook-form.</p>
      </div>
    </div>
  );
};

export default ManagerAddLoan;
